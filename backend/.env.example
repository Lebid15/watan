# Backend Environment Example (no secrets)
# Copy to .env and fill real values. DO NOT COMMIT real .env.

# --- Core ---
DATABASE_URL=postgres://watan:password@postgres:5432/watan?sslmode=disable
REDIS_URL=redis://redis:6379/0
JWT_SECRET=change-me-please
PUBLIC_TENANT_BASE_DOMAIN=wtn4.com
AUTO_MIGRATIONS=false
HOST=0.0.0.0
PORT=3000

# --- Bootstrap (disable after first run) ---
INITIAL_ROOT_EMAIL=owner@example.com
INITIAL_ROOT_PASSWORD=ChangeMe123!
BOOTSTRAP_ENABLED=false

# --- Passkeys / WebAuthn ---
RP_ID=wtn4.com
PASSKEYS_STRICT=true

# --- Frontend ---
NEXT_PUBLIC_API_URL=https://api.wtn4.com/api

# --- Email (SMTP) ---
# Choose one provider example and set values in real .env
# Brevo (Sendinblue) example:
# EMAIL_HOST=smtp-relay.brevo.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=YOUR_SMTP_LOGIN   # e.g. 968ce0001@smtp-brevo.com
# EMAIL_PASS=YOUR_SMTP_PASSWORD
# EMAIL_FROM="Watan Store <no-reply@wtn4.com>"
# FRONTEND_URL=https://wtn4.com

# Temporary (using a verified Gmail sender while domain sender not yet verified):
# EMAIL_FROM="wtn4 <lebid.hac.alaye@gmail.com>"

# SendGrid example:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASS=SG.xxxxxxxx
# EMAIL_FROM="Watan Store <no-reply@wtn4.com>"
# FRONTEND_URL=https://wtn4.com

