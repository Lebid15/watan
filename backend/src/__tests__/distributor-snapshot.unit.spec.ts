// Wrapper to run distributor snapshot unit test within src tree (so rootDir=src picks it up)
// We import the original test file to avoid duplication.
import '../../test/distributor-snapshot.unit.spec';
