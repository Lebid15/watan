# ========================================
# Django Environment Configuration
# ========================================
# ⚠️ للإنتاج: قم بتغيير جميع القيم الافتراضية!
# ⚠️ FOR PRODUCTION: Change ALL default values!

# ======== Django Core Settings ========
# توليد SECRET_KEY جديد: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=change-this-to-a-long-random-string-in-production

# في الإنتاج: يجب أن يكون 0
# In Production: MUST be 0
DJANGO_DEBUG=0

# في الإنتاج: حدد النطاقات المسموحة فقط
# In Production: Specify only allowed domains
# Example: DJANGO_ALLOWED_HOSTS=wtn4.com,*.wtn4.com,api.wtn4.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# ======== Environment Type ========
# قيم مقبولة: development, production
# Accepted values: development, production
ENVIRONMENT=development

# ======== Database Settings ========
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=watan
POSTGRES_USER=watan
# ⚠️ في الإنتاج: استخدم كلمة مرور قوية جداً!
# Generate: openssl rand -base64 32
POSTGRES_PASSWORD=changeme-use-strong-password-in-production

# ======== Redis ========
REDIS_URL=redis://redis:6379/0

# ======== JWT Settings ========
# توليد JWT_SECRET جديد: openssl rand -base64 64
JWT_SECRET=change-this-to-a-long-random-string-in-production
JWT_ACCESS_MIN=60
JWT_REFRESH_DAYS=7

# ======== Public Settings ========
PUBLIC_TENANT_BASE_DOMAIN=wtn4.com

# ZNet adapter endpoint overrides (optional)
# If provider mounts under /servis by default, you can omit these.
DJ_ZNET_CATALOG_PATH=servis/pin_listesi.php
DJ_ZNET_ORDERS_PATH=servis/pin_ekle.php
DJ_ZNET_STATUS_PATH=servis/pin_kontrol.php
# Optional balance path (provider may not expose a balance API). If set, balance refresh will use it.
# DJ_ZNET_BALANCE_PATH=servis/bakiye.php
