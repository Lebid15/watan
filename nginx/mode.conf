## Runtime maintenance switch file
## Included inside each server {} block (see nginx.conf)
## Only 'set' directives are allowed there (no 'map').
## Backend toggle script rewrites this atomically.
# default state
set $maintenance_switch off;
